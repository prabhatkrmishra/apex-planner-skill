# Phase 06 — Quality + Handoff
Run the plan quality gate, validate the plan artifact, then hand off to `apex-godmode`. Do not implement or duplicate Godmode logic. Godmode must re-check assumptions before editing.
