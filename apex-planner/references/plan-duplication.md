# Plan Duplication Control

Before creating a plan, search the repository for an existing plan, ADR, issue/spec, RFC, design doc or task file for the same change.

Prefer updating the existing canonical planning artifact when the repository convention allows it. Do not create parallel plans that can diverge.

A plan may reference existing requirements/ADRs rather than copying them. Record only the delta introduced by the new change.
