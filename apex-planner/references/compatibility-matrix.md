# Compatibility Matrix

For public APIs, events, schemas, packages, plugins, SDKs and rolling deployments, model version skew explicitly.

Minimum matrix where relevant:
- old consumer × new provider;
- new consumer × old provider;
- mixed service versions;
- rollback after partial rollout.

Record supported combinations, migration windows, deprecation dates and rejection behavior.
