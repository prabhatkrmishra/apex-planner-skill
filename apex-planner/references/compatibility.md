# Compatibility Planning

Check APIs, schemas, serialized data/events, client/server skew, configuration, dependencies, runtime/OS/browser support, plugins/extensions, and deployment sequencing.

For upgrades state current version, target, support lifecycle, required migration steps and deprecations.
