# Plan Lifecycle

Use statuses: `draft`, `researching`, `ready`, `blocked`, `superseded`.

Record plan revision and assumptions that changed. Do not silently overwrite a previously approved decision when new evidence arrives; note the changed evidence and decision impact.

When implementation begins, the plan becomes the implementation contract but remains revisable if repository evidence invalidates it.
