# Event / Messaging Planning

Specify producer/consumer ownership, message schema, versioning, delivery semantics, ordering, deduplication, idempotency, retries, dead-letter behavior, poison messages, backpressure, replay, observability and compatibility during rollout.

Never assume exactly-once semantics without verifying the actual platform and consumer behavior.
