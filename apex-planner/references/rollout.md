# Rollout Planning

Choose rollout controls based on blast radius: dark launch, feature flag, canary, percentage rollout, staged environment, or direct release. Define success/abort thresholds, monitoring interval, rollback action, data implications, operator ownership, and cleanup.

A rollout is incomplete if the team can enable a feature but cannot confidently disable or compensate for it.
