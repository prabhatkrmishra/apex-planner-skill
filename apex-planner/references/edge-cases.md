# Edge-Case Matrix

Select relevant cases: malformed/null/empty/oversize input; duplicates/replay; race/deadlock; timeout/partial failure; dependency degradation; stale cache; ordering/eventual consistency; rate limits/quotas; pagination; old/new client skew; schema precision/timezone/locale; authz/tenancy; PII/secrets; browser/mobile/offline/reconnect/accessibility; background jobs; generated code; observability failures; rollout skew; rollback after partial migration; restore/DR.
