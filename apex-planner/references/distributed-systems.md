# Distributed Systems Planning

Evaluate delivery semantics, duplicates, ordering, idempotency, retries, backoff/jitter, timeout/cancellation, partial failure, stale reads, cache invalidation, partition behavior, rate limits, leases/fencing, poison messages, dead letters, compensation and replay safety.

Any cross-process side effect must have a clear duplicate/retry story.
