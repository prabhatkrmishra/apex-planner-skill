# Database Planning

Define access patterns before schema shape. Check keys/indexes, cardinality, constraints, transaction boundaries, isolation, locking/concurrency, query plans where material, nullability/defaults, precision/timezone, retention, backups/restores and migration order.

For destructive or irreversible changes, provide validation and compensation/restore strategy before cleanup.
